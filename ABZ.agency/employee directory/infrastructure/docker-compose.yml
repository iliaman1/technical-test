version: '3.8'

services:
  db:
    image: postgres:14-alpine
    container_name: employee_db
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/

volumes:
  postgres_data:
